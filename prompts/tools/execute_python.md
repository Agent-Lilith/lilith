## Description

Execute Python code in a safe, isolated sandbox. Files saved to the current directory persist between calls in the same session.

## Examples

```json
{"tool": "execute_python", "code": "print(2 + 2)"}
```

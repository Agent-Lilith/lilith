## Description

Get a full email thread by Gmail thread ID. Returns thread_id, subject, message_count, and messages array. Use after emails_search when the user wants the full conversation.

## Examples

```json
{"tool": "email_get_thread", "thread_id": "<thread_id>"}
```

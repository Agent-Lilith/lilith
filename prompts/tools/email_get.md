## Description

Get a single email by its Gmail message ID. Use after universal_search when you need full details of a specific message (use the id from search results).

## Examples

```json
{"tool": "email_get", "email_id": "<message_id>"}
```
